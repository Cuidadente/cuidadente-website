# CuidaDente — environment variables (copy to `.env` and adjust).
#
# The API is a SEPARATE app. The frontend only calls it on FORM SUBMIT
# (see src/lib/api.ts) — page loads are 100% local/static and make no request.
#
# Base URL of the API app that the forms POST to.
PUBLIC_API_BASE_URL=https://example.com
